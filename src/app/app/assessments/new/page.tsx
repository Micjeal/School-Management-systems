import { requireUserContext } from "@/lib/auth/context";import { createClient } from "@/lib/supabase/server";import { PageHeader } from "@/components/layout/page-header";import { Card,CardContent } from "@/components/ui/card";import { Input,Label,Select,Textarea } from "@/components/ui/input";import { Button } from "@/components/ui/button";import { createAssessment } from "../actions";
export default async function NewAssessment({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;const c=await requireUserContext("assessments.manage");const s=await createClient();const sid=c.active_school_id;const tables=await Promise.all(["academic_years","terms","class_sections","subjects","assessment_types","grading_scales"].map(t=>(s.from(t as any) as any).select("*").eq("school_id",sid)));const [years,terms,sections,subjects,types,scales]=tables.map(x=>x.data??[]);return <div><PageHeader title="New assessment" description="Define the class, subject, maximum score and weighting before mark entry." backHref="/app/assessments"/>{p.error?<p className="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{p.error}</p>:null}<Card><CardContent><form action={createAssessment} className="grid gap-5 md:grid-cols-3"><div className="md:col-span-2"><Label>Title</Label><Input name="title" required/></div><div><Label>Assessment date</Label><Input name="assessment_date" type="date"/></div><Sel n="academic_year_id" l="Academic year" rows={years}/><Sel n="term_id" l="Term" rows={terms}/><Sel n="class_section_id" l="Class section" rows={sections.map((x:any)=>({...x,name:x.name}))}/><Sel n="subject_id" l="Subject" rows={subjects}/><Sel n="assessment_type_id" l="Assessment type" rows={types}/><Sel n="grading_scale_id" l="Grading scale" rows={scales} optional/><div><Label>Maximum score</Label><Input name="maximum_score" type="number" min="1" step="0.01" required/></div><div><Label>Weight</Label><Input name="weight" type="number" min="0" step="0.01" defaultValue="100" required/></div><div className="md:col-span-3"><Label>Description</Label><Textarea name="description"/></div><div className="md:col-span-3 flex justify-end"><Button>Create assessment</Button></div></form></CardContent></Card></div>}
function Sel({n,l,rows,optional=false}:{n:string;l:string;rows:any[];optional?:boolean}){return <div><Label>{l}</Label><Select name={n} required={!optional}><option value="">Select</option>{rows.map((x:any)=><option value={x.id} key={x.id}>{x.name}</option>)}</Select></div>}
