"use server";import { redirect } from "next/navigation";import { revalidatePath } from "next/cache";import { createClient } from "@/lib/supabase/server";import { requireUserContext } from "@/lib/auth/context";const v=(f:FormData,k:string)=>String(f.get(k)??"").trim();
export async function createAssessment(f:FormData){const c=await requireUserContext("assessments.manage");const s=await createClient();const {data,error}=await (s.from("assessments") as any).insert({school_id:c.active_school_id,academic_year_id:v(f,"academic_year_id"),term_id:v(f,"term_id"),class_section_id:v(f,"class_section_id"),subject_id:v(f,"subject_id"),assessment_type_id:v(f,"assessment_type_id"),title:v(f,"title"),description:v(f,"description")||null,assessment_date:v(f,"assessment_date")||null,maximum_score:Number(v(f,"maximum_score")),weight:Number(v(f,"weight")||100),grading_scale_id:v(f,"grading_scale_id")||null,status:"draft",created_by:c.user_id}).select("id").single();if(error)redirect(`/app/assessments/new?error=${encodeURIComponent(error.message)}`);revalidatePath("/app/assessments");redirect(`/app/assessments/${data.id}`)}
export async function saveMarks(id:string,f:FormData){await requireUserContext("results.enter");const s=await createClient();const {error}=await s.rpc("save_mark_entries" as any,{target_assessment_id:id,entries:JSON.parse(v(f,"entries")||"[]"),submit_entries:f.get("submit_entries")==="yes"} as any);if(error)redirect(`/app/assessments/${id}?error=${encodeURIComponent(error.message)}`);revalidatePath(`/app/assessments/${id}`);redirect(`/app/assessments/${id}?message=Marks%20saved`)}
export async function moderateMarks(id:string,f:FormData){await requireUserContext("results.moderate");const s=await createClient();const approve=f.get("approve")==="yes";const {error}=await s.rpc("moderate_assessment_marks" as any,{target_assessment_id:id,approve,note:v(f,"note")||null} as any);if(error)redirect(`/app/assessments/${id}?error=${encodeURIComponent(error.message)}`);revalidatePath(`/app/assessments/${id}`);redirect(`/app/assessments/${id}?message=Moderation%20completed`)}
export async function calculateResults(f:FormData){const c=await requireUserContext("results.moderate");const s=await createClient();const {error}=await s.rpc("calculate_class_results" as any,{target_school_id:c.active_school_id,target_academic_year_id:v(f,"academic_year_id"),target_term_id:v(f,"term_id"),target_class_section_id:v(f,"class_section_id")} as any);if(error)redirect(`/app/assessments?error=${encodeURIComponent(error.message)}`);revalidatePath("/app/assessments");redirect("/app/assessments?message=Results%20and%20report%20cards%20calculated")}
export async function publishResults(f:FormData){await requireUserContext("results.publish");const s=await createClient();const {data:publication,error:createError}=await (s.from("result_publications") as any).insert({school_id:v(f,"school_id"),academic_year_id:v(f,"academic_year_id"),term_id:v(f,"term_id"),class_section_id:v(f,"class_section_id"),publication_type:"term_results",status:"draft"}).select("id").single();if(createError)redirect(`/app/assessments?error=${encodeURIComponent(createError.message)}`);const {error}=await s.rpc("publish_class_results" as any,{publication_id:publication.id} as any);if(error)redirect(`/app/assessments?error=${encodeURIComponent(error.message)}`);revalidatePath("/app/assessments");redirect("/app/assessments?message=Results%20published")}
