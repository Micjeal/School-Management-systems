"use server";import { redirect } from "next/navigation";import { revalidatePath } from "next/cache";import { createClient } from "@/lib/supabase/server";import { requireUserContext } from "@/lib/auth/context";const v=(f:FormData,k:string)=>String(f.get(k)??"").trim();
export async function decideApplication(id:string,f:FormData){const c=await requireUserContext("admissions.manage");const decision=v(f,"decision");const s=await createClient();const {error}=await (s.from("applications") as any).update({status:decision,decision,decision_at:new Date().toISOString(),decision_by:c.user_id,notes:v(f,"notes")||null}).eq("id",id).eq("school_id",c.active_school_id);if(error)redirect(`/app/admissions/${id}?error=${encodeURIComponent(error.message)}`);revalidatePath(`/app/admissions/${id}`);redirect(`/app/admissions/${id}?message=Decision%20saved`)}
export async function convertApplication(id:string,f:FormData){await requireUserContext("admissions.manage");const s=await createClient();const {data,error}=await s.rpc("convert_application_to_student" as any,{target_application_id:id,admission_number:v(f,"admission_number"),class_section_id:v(f,"class_section_id"),target_term_id:v(f,"term_id")||null,roll_number:v(f,"roll_number")||null} as any);if(error)redirect(`/app/admissions/${id}?error=${encodeURIComponent(error.message)}`);revalidatePath("/app/admissions");redirect(`/app/students/${(data as any).student_id}?message=Applicant%20enrolled`)}
