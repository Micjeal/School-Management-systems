import { requireUserContext } from "@/lib/auth/context";import { createClient } from "@/lib/supabase/server";import { PageHeader } from "@/components/layout/page-header";import { Card,CardContent,CardHeader } from "@/components/ui/card";import { Input,Label,Select } from "@/components/ui/input";import { Button } from "@/components/ui/button";import { createStudent } from "../actions";
export default async function NewStudent({searchParams}:{searchParams:Promise<{error?:string}>}){const c=await requireUserContext("students.create");const p=await searchParams;const s=await createClient();const sid=c.active_school_id;const [{data:campuses},{data:years},{data:terms},{data:sections}]=await Promise.all([(s.from("campuses") as any).select("id,name").eq("school_id",sid).eq("is_active",true),(s.from("academic_years") as any).select("id,name").eq("school_id",sid).order("starts_on",{ascending:false}),(s.from("terms") as any).select("id,name").eq("school_id",sid).order("starts_on",{ascending:false}),(s.from("class_sections") as any).select("id,name,class_groups(name)").eq("school_id",sid).eq("status","active")]);return <div><PageHeader title="Admit student" description="Creates the person, student and optional class enrolment in one protected transaction." backHref="/app/students"/>{p.error?<p className="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{p.error}</p>:null}<form action={createStudent} className="space-y-6"><Card><CardHeader><h2 className="font-semibold">Personal information</h2></CardHeader><CardContent className="grid gap-5 md:grid-cols-3"><F n="first_name" l="First name"/><F n="middle_name" l="Middle name" optional/><F n="last_name" l="Last name"/><D n="date_of_birth" l="Date of birth"/><div><Label>Gender</Label><Select name="gender"><option value="">Select</option><option>female</option><option>male</option><option>other</option><option>prefer_not_to_say</option></Select></div><F n="nationality_code" l="Nationality code" d="UG"/><F n="primary_email" l="Email" type="email" optional/><F n="primary_phone" l="Phone" optional/></CardContent></Card><Card><CardHeader><h2 className="font-semibold">Admission and enrolment</h2></CardHeader><CardContent className="grid gap-5 md:grid-cols-3"><F n="admission_number" l="Admission number"/><F n="student_number" l="Student number" optional/><D n="admission_date" l="Admission date"/><div><Label>Boarding status</Label><Select name="boarding_status" defaultValue="day"><option>day</option><option>boarding</option><option>weekly_boarding</option></Select></div><Sel n="current_campus_id" l="Campus" rows={campuses}/><Sel n="academic_year_id" l="Academic year" rows={years} optional/><Sel n="term_id" l="Term" rows={terms} optional/><Sel n="class_section_id" l="Class section" rows={(sections??[]).map((x:any)=>({id:x.id,name:`${x.class_groups?.name??"Class"} — ${x.name}`}))} optional/><F n="roll_number" l="Roll number" optional/></CardContent></Card><div className="flex justify-end"><Button>Admit student</Button></div></form></div>}
function F({n,l,d,type="text",optional=false}:{n:string;l:string;d?:string;type?:string;optional?:boolean}){return <div><Label>{l}{optional?"":" *"}</Label><Input name={n} type={type} defaultValue={d} required={!optional}/></div>}function D({n,l}:{n:string;l:string}){return <div><Label>{l}</Label><Input name={n} type="date" defaultValue={new Date().toISOString().slice(0,10)} required/></div>}function Sel({n,l,rows,optional=false}:{n:string;l:string;rows:any[]|null;optional?:boolean}){return <div><Label>{l}</Label><Select name={n} required={!optional}><option value="">Select…</option>{(rows??[]).map((r:any)=><option value={r.id} key={r.id}>{r.name}</option>)}</Select></div>}
