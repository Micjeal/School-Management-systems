import { Card, CardContent } from "@/components/ui/card"; import { Input,Label } from "@/components/ui/input"; import { Button } from "@/components/ui/button"; import { changePasswordAction } from "./actions";
export default async function ChangePassword({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams;return <main className="flex min-h-screen items-center justify-center p-6"><div className="w-full max-w-md"><h1 className="text-3xl font-bold">Secure your account</h1><p className="mt-2 text-sm text-slate-500">Replace the temporary password before using SchoolDB.</p><Card className="mt-6"><CardContent><form action={changePasswordAction} className="space-y-5">{p.error?<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{p.error}</p>:null}<div><Label>New password</Label><Input name="password" type="password" minLength={10} required/></div><div><Label>Confirm password</Label><Input name="confirm" type="password" minLength={10} required/></div><Button className="w-full">Save new password</Button></form></CardContent></Card></div></main>}
