"use client";import { useMemo,useState } from "react";import { Button } from "@/components/ui/button";
type Row={id:string;student_id:string;attendance_status:string;minutes_late:number;reason:string|null;students?:{admission_number:string;people?:{first_name:string;last_name:string}}};
const statuses=["present","absent","late","excused","medical","remote","not_required"];
export function AttendanceGrid({rows,action,locked}:{rows:Row[];action:(formData:FormData)=>void|Promise<void>;locked:boolean}){const [records,setRecords]=useState(rows.map(r=>({student_id:r.student_id,attendance_status:r.attendance_status,minutes_late:r.minutes_late,reason:r.reason??""})));const payload=useMemo(()=>JSON.stringify(records),[records]);const update=(i:number,key:string,value:string|number)=>setRecords(x=>x.map((r,n)=>n===i?{...r,[key]:value}:r));return <form action={action}><input type="hidden" name="records" value={payload}/><div className="overflow-x-auto rounded-2xl border bg-white"><table className="min-w-full divide-y"><thead className="bg-slate-50"><tr><th className="px-4 py-3 text-left text-xs font-semibold uppercase text-slate-500">Student</th><th className="px-4 py-3 text-left text-xs font-semibold uppercase text-slate-500">Status</th><th className="px-4 py-3 text-left text-xs font-semibold uppercase text-slate-500">Minutes late</th><th className="px-4 py-3 text-left text-xs font-semibold uppercase text-slate-500">Reason</th></tr></thead><tbody className="divide-y">{rows.map((row,i)=><tr key={row.student_id}><td className="px-4 py-3"><p className="font-semibold">{row.students?.people?.first_name} {row.students?.people?.last_name}</p><p className="text-xs text-slate-500">{row.students?.admission_number}</p></td><td className="px-4 py-3"><select disabled={locked} value={records[i]!.attendance_status} onChange={(e:React.ChangeEvent<HTMLSelectElement>)=>update(i,"attendance_status",e.target.value)} className="h-10 rounded-xl border px-3 text-sm">{statuses.map(s=><option key={s}>{s}</option>)}</select></td><td className="px-4 py-3"><input disabled={locked} type="number" min="0" value={records[i]!.minutes_late} onChange={(e:React.ChangeEvent<HTMLInputElement>)=>update(i,"minutes_late",Number(e.target.value))} className="h-10 w-24 rounded-xl border px-3 text-sm"/></td><td className="px-4 py-3"><input disabled={locked} value={records[i]!.reason} onChange={(e:React.ChangeEvent<HTMLInputElement>)=>update(i,"reason",e.target.value)} className="h-10 min-w-52 rounded-xl border px-3 text-sm"/></td></tr>)}</tbody></table></div>{!locked?<div className="mt-5 flex justify-end gap-3"><Button variant="secondary" name="submit_session" value="no">Save draft</Button><Button name="submit_session" value="yes">Save and submit</Button></div>:null}</form>}
